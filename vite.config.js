
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    outDir: 'dist', // Customize the publish directory here
  },
  // Other Vite configuration options can be added here as needed
});